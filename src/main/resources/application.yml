spring:
  datasource:
    url: jdbc:mysql://localhost:3306/clothify
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
  mail:
    host: smtp.gmail.com
    port: 587
    username: clothify.web@gmail.com
    password: ${mailsender}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  secret: ${HEX_KEY_01}